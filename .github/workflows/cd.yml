name: CD Pipeline

on:
  workflow_run:
    workflows: ["CI Pipeline"]
    types:
      - completed

jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: Pull Docker Image
        run: docker pull abrarkhan1911/abrarapp

      - name: Stop and Remove Existing Docker Container (if exists)
        run: |
          docker stop abrarapp-container || true
          docker rm abrarapp-container || true

      - name: Run Docker Container
        run: |
          docker run -d -p 8080:8080 --name abrarapp-container abrarkhan1911/abrarapp
